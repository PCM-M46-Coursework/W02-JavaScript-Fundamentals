/**
 * First Things First!
 * Display the 8th character of this sentence in upper case on the console.
 */
const Activity0 = () =>
{
    // Arrange
    const str = "All Around the World";

    // Act
    const char = str[7].toUpperCase();

    // Assert
    console.log(char);
}

Activity0();